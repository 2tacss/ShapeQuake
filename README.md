# ShapeQuake

ShapeQuake is a Quake-style terminal featuring non-rectangular windows, where the shape and rendering dynamically adapt based on the executed command. 
It features custom implementations of both the Shell and the Terminal.

Currently, the Shell is under development.

## Directory Structure
```
.
├── include
│   ├── common.h
│   └── defines.h
├── shell
│   ├── include
│   ├── src
│   └── Makefile
├── terminal
│   ├── include
│   ├── src
│   └── Makefile
├── ui
│   ├── src
│   └── Makefile
├── main.c
└── Makefile
```
